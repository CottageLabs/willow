<div class="form-group multi-nested rdss_cdm_people">
  <%# only output the label and the hint text. wrapper defined in config/initializers/simple_form_rdss.rb %>
  <%= f.input :object_people, wrapper: :label_and_hint_only, required: f.object.required?(:object_people) %>
  <ul class="listing"><%# normally added by https://github.com/samvera/hydra-editor/blob/master/app/inputs/multi_value_input.rb %>
    <%= f.fields_for :object_people, @form.object_people, builder: ::ObjectPersonFormBuilder do |form| %>
      <%= render partial: form %>
    <% end %>
  </ul>
  <%= add_another_button :object_person %>
</div>